name: projection_baseline

audio:
  input_dim: null
  hidden_dim: 256
  output_dim: 128

text:
  input_dim: null
  hidden_dim: 256
  output_dim: 128

similarity:
  temperature: 0.07
  normalize: true
